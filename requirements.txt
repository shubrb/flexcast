# Phase 0-1 (data)
# gridstatus pinned: engines/download.py's current-year fallbacks reuse its
# internals (parse_doc, _finalize_spp_df, _process_post_settlements_load_data)
gridstatus==0.36.0
pandas>=2.0
pyarrow>=14.0
requests>=2.31
PyYAML>=6.0

# Phase 3+ (forecaster / planner) — installed now so one setup serves the whole week
lightgbm>=4.0
scikit-learn>=1.3
pulp>=2.7
matplotlib>=3.8
